# Security

Report security issues privately to the repository owner. Do not include secrets, wallet private keys, OAuth refresh tokens, RPC keys, or user personal data in issues or pull requests.

The MVP uses mock adapters only. Production integrations must use official YouTube APIs, read-only stream-scoped overlay tokens, KMS or Secret Manager for secrets, chain confirmation windows, and audit logs for all admin actions.
