# Change Classification

Head inspected: `afb371ef29fb7fea9e5cc08fef866040441e3b79`.

This file classifies every PR #2 change surface so `classificationCoverageStatus` has no unknown files.

| Classification | Files | Risk | Verification | Residual risk |
| --- | --- | --- | --- | --- |
| `machine_readable_quality_evidence` | `.codex/change-classification.json`, `.codex/task-contract.json`, `.codex/product-verification.json`, `.codex/test-coverage-evidence.json`, `.codex/review-independence.json`, `.codex/quality-gate-evidence.json` | Quality-gate may not parse prose-only docs. | Machine-readable evidence mirrors PR body and docs; JSON is included in changed file classification. | Harness parser support must be verified by rerunning quality-gate. |
| `db_schema` | `migrations/0001_durable_events.sql`, `apps/api/src/db/migration.test.ts` | Incorrect uniqueness or retention model could break idempotency or privacy. | `corepack pnpm test` covers required tables and unique constraints. | Live PostgreSQL migration execution is not covered yet. |
| `repository_boundary` | `apps/api/src/repositories/types.ts`, `apps/api/src/repositories/in-memory.ts`, `apps/api/src/repositories/in-memory.test.ts`, `apps/api/src/repositories/postgres.ts`, `apps/api/src/repositories/postgres.test.ts` | API could depend on in-memory internals or SQL could expose raw viewer data. | Repository tests cover idempotency, public DTO safety, default affinity, and parameterized SQL. | Full Postgres pool wiring and live DB tests remain future work. |
| `api_internal_flow` | `apps/api/src/server.ts`, `apps/api/src/server.test.ts` | Duplicate events or unsafe moderation statuses could emit side effects. | API tests cover duplicate events, injected repository use, moderation gates, admin auth, overlay token rejection, and public DTO safety. | Production adapters are still mocked. |
| `outbox_dlq_boundary` | `apps/api/src/outbox/worker.ts`, `apps/api/src/outbox/worker.test.ts` | At-least-once delivery without idempotency can duplicate effects. | Worker tests cover claim, retry, completed job exclusion, active lock respect, and DLQ movement. | Stale lock reclamation and admin DLQ retry endpoint are not implemented. |
| `config_validation` | `apps/api/src/config/env.ts`, `apps/api/src/config/env.test.ts` | Production could accidentally use local mock defaults. | Config tests cover local/test allow and production reject behavior. | Real secret source management remains deployment work. |
| `docs` | `docs/API_CONTRACT.md`, `docs/ARCHITECTURE.md`, `docs/AUDIT_REPORT_DB_QUEUE.md`, `docs/DEPENDENCY_DECISIONS.md`, `docs/DEV_ENVIRONMENT.md`, `docs/EVENT_SCHEMA.md`, `docs/IRIS_INTEGRATION.md`, `docs/RUNBOOK.md`, `docs/SECURITY.md`, `docs/THREAT_MODEL.md`, `docs/pr-bootstrap-mvp.md`, `docs/pr-durable-events-db-queue.md`, plus quality evidence docs | Incorrect docs could imply production readiness or prohibited financial behavior. | Product and security scans plus review of PR body. | Docs must be updated again after live DB and production integrations. |
| `tests` | `apps/api/src/server.test.ts`, `apps/api/src/repositories/*.test.ts`, `apps/api/src/outbox/worker.test.ts`, `apps/api/src/db/migration.test.ts` | Missing tests could allow regressions in idempotency or public data exposure. | `corepack pnpm test`: 9 test files, 45 tests. | No live DB integration tests yet. |
| `web_safety_copy_testability` | `apps/web/app/safety.ts`, `apps/web/app/page.tsx`, `apps/web/app/page.test.ts` | UI safety text tests could depend on TSX transform behavior in root npm diagnostics. | `npm test` and `corepack pnpm test`: 9 test files, 45 tests. | No production UI browser smoke in PR #2. |
| `ci_contract_dependency_pinning` | `.github/workflows/ci.yml`, `docs/DEPENDENCY_DECISIONS.md` | Default branch dependency resolution can make contract CI nondeterministic. | GitHub CI contracts job passed on PR #2 after pinning. | Local Windows environment does not have Foundry installed. |
| `env_example` | `.env.example` | Example values could be mistaken for production values. | `loadConfig` rejects generated local mock defaults in production-like mode. | Production secret injection and rotation are deployment tasks. |
| `migration` | `migrations/0001_durable_events.sql` | Schema drift could break idempotency or auditability. | Migration tests assert table and unique constraint presence. | Status text columns need enum check constraints in a follow-up migration. |
| `package_manager_compatibility` | `package.json`, `pnpm-lock.yaml`, `vitest.config.ts`, workspace package manifests if changed by quality repair | Quality-gate npm entry can diverge from pnpm CI. | `corepack pnpm install`, `corepack pnpm lint`, `corepack pnpm typecheck`, `corepack pnpm test`, `npm test`, and clean `npm install --no-package-lock && npm test` reproduction. | If GitHub quality-gate still fails, inspect remote npm diagnostic artifact. |
| `node20_test_compatibility` | `apps/api/src/server.test.ts`, `package.json`, `pnpm-lock.yaml` | Remote quality-gate runs Node 20 and does not provide the newer global `WebSocket` test surface. | Node 20 local reproduction passed with 9 test files and 45 tests after explicit `ws` import and rejected connection handling. | This is a test compatibility dependency only; production overlay server behavior is unchanged. |
